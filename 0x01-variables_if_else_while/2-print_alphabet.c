#include  <stdio.h>

/**
 * main - ENtry Point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int alphabet;
	for (alphabet = 'A'; alphabet <= 'Z'; alphabet++)
	{
		putchar(tolower(x));
	}
		putchar('\n');
		return (0);
}
