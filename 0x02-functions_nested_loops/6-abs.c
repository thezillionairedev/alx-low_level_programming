#include "main.h"

/**
 * _abs - unction that computes the absolute value of an integer
 * @n: Integer
 *
 * Return: Always 0 (Success)
 */
int _abs(int n)
{
	if (n > 0 || n < 0)
	{
		return (n);
	}
	return (0);
}
